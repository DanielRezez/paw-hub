name: projeto_integrador2
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  firebase_core: ^4.1.0
  firebase_auth: ^6.0.2
  google_fonts: ^6.3.1
  google_sign_in: ^6.2.1
  flutter_svg: ^2.2.1
  cupertino_icons: ^1.0.8
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:

  uses-material-design: true
  assets:
      - assets/images/google_logo.svg
